import React, { useEffect } from "react";
import ProfileForm from "../common/ProfileForm";

const SignUp = () => {
  const isSignUp = true;

  return <ProfileForm isSignUp={isSignUp} />;
};

export default SignUp;
